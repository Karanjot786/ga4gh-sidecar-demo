FROM python:3.12-slim

WORKDIR /app
COPY mock_backend/ mock_backend/
RUN pip install --no-cache-dir fastapi uvicorn[standard]

EXPOSE 9090

CMD ["uvicorn", "mock_backend.app:app", "--host", "0.0.0.0", "--port", "9090"]
